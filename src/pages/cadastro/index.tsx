import React from 'react';
import { Typography, Container } from '@material-ui/core';

export const CadastroScreen = () => {
    return (
        <Container>
            <Typography>Criar cadastro de usuário interno.</Typography>
        </Container>
    )
}